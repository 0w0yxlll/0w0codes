#0w0codes

#include<cstdio>
#include<iostream>
#include<algorithm>
#define maxn 100
using namespace std;

bool cmp(int x,int y)
{
	return x<y	;
}

int main()
{
	int n,a[maxn];
	cin>>n;
	for(int i=0;i<n;i++)
		cin>>a[i];
	sort(a,a+n,cmp);
	for(int i=0;i<=n-1;i++) 
		cout<<a[i]<<" ";
	cout<<endl;
	return 0;
}
