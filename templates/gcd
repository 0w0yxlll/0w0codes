#include<iostream>
using namespace std;
#define i64 int

i64 a,b;

i64 gcd(i64 x,i64 y){
	if(y==0) return x;
	else return gcd(y,x%y);
}

int main(){
	while(cin>>a>>b)
		cout<<gcd(a,b)<<endl;
	return 0;	
}
