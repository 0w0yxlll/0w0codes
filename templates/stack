#include<iostream>
#include<stack>
using namespace std;

stack <int> a;
int n;

int main(){
	cin>>n;
	for(int i=1;i<=n;i++){
		int t;
		cin>>t;
		a.push(t);
	}
	while(!a.empty()){
		cout<<a.top()<<" ";
		a.pop();
	}
	cout<<endl;
	return 0;
}
